Things to do:
  - student details page
    - delete button
Things to check out:
  - js bubbling

Things to consider next:
  - how to design the teachers page?
  - how to design the groups page?
  - how to design the management page?

Things to skip:
  - about school page 
  - contact page

file .env (must be in .gitignore)
  keep secrets 
    vite_server_url = address (http://localhost:8080/)

    the idea is to keep the ip adress as a global var and use it across the app



done: 
 - student page (done)
    - finish the student adding window - clear fields after clicking cancel
    - change the blue color when mouse over filter bar (would be good to change in the global css)
    - highlight the paragrapghs (or anchors) when mouse over (could also be done in the global css)
    - create 2 or 3 columns out of students
    - add the redirection to new page after clicking "Zapisz"
    - handle the one template many subpages logic (there should be a subpage for each student with details about them, how to handle it?)

  - handle .env files 
    - added .env.developemet and .env.production
    - create the var for dev and production BE API_URL
    - used newly created var in js scripts that called BE
  
  - student details page
    - create data sections
    - create input fields with label
    - fetach data from BE API and fill the input fields
    - add edit/save button
    - handle the edit/save logic
    - call the BE API to save student details