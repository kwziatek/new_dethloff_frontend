Things to do:
 - student page
    - change the blue color when mouse over filter bar (would be good to change in the global css)
    - highlight the paragrapghs (or anchors) when mouse over (could also be done in the global css)

Things to check out:
 -

Things to consider next:
  - how to design the students page? Initial idea: lets call for all of the students to BE and display them (part of them). Let's also add a search bar that would allow to search after name or surname. Obviously the results (list of students) would be shown accordingly.
  - how to design the teachers page?
  - how to design the groups page?
  - how to design the management page?

Things to skip:
  - about school page 
  - contact page

  file .env (must be in .gitignore)
    keep secrets 
      vite_server_url = adres (http://localhost:8080/)

      the idea is to keep the be ip adress as a global var and use it across the app

include the flyway scripts in the .gitignore